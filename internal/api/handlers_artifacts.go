// handlers_artifacts.go — artifact endpoints reuse PostObject("artifact") and
// ListObjects("artifact") registered in router.go. No additional artifact-specific
// logic is required in v0.1.
package api
